#!/bin/sh

cd `pwd`
exec java -Djava.util.logging.config.file=@datadir@/logger.properties \
          @JAVA_RUNTIME_FLAGS@ -cp @prefix@/jar/pipeline.jar \
          us/temerity/pipeline/FileMgrApp "$@"

