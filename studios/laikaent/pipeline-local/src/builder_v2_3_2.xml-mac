	<target name="installMacOS" description="installs the builder scripts into pipeline">
		<mkdir dir="${MacDir}"/>
		<copy todir="${MacDir}">
			<fileset dir="${ScriptsDir}/MacOS"></fileset>
		</copy>
	</target>
